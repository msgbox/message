MsgBox Message Protocol Buffer
=================================

A struct using the protocol buffer format. It's used when Marshalling and Unmarshalling messages from a queue.

Used in various MsgBox packages.